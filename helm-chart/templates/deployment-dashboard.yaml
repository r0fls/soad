apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-of-a-dow-dashboard
  labels:
    app: system-of-a-dow-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: system-of-a-dow-dashboard
  template:
    metadata:
      labels:
        app: system-of-a-dow-dashboard
    spec:
      containers:
        - name: system-of-a-dow-dashboard
          image: r0fls/soad-trading-dashboard:latest
          imagePullPolicy: {{ .Values.dashboard.image.pullPolicy }}
          ports:
            - containerPort: 80
          env:
            - name: API_URL
              value: "http://trading-api-service:8080"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
